{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "docs": "lerna run docs",
    "publish": "lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^8.1.3"
  }
}
